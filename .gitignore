# Dependencies & build output
node_modules/
out/
dist/
coverage/

# Local databases & backups
*.db
*.db-journal
*.db-wal
*.db-shm
*.bak

# Credentials & secrets (never commit)
credentials/
client_secret*.json
tokens*.json
*.enc
.env
.env.*

# Logs & OS noise
*.log
Thumbs.db
.DS_Store
dist-dev/
dist-installer/
dist-release/
test-results/
playwright-report/

# One person's folder of real videos, for the end-to-end tests. See e2e/fixtures.ts.
e2e/sample-dir.txt

# TypeScript's incremental build cache. Rewritten on every typecheck, so tracking it only ever
# made the working tree look changed.
*.tsbuildinfo
