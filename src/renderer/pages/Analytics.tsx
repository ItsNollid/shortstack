import React from 'react';
import { Placeholder } from './Placeholder';

export function Analytics(): React.JSX.Element {
  return (
    <Placeholder
      title="Analytics"
      subtitle="How your Shorts are doing"
      next="Views, watch time and subscriber change come back once the channel data screen is rebuilt."
    />
  );
}
