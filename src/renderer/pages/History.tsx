import React from 'react';
import { Placeholder } from './Placeholder';

export function History(): React.JSX.Element {
  return (
    <Placeholder
      title="History"
      subtitle="Everything ShortStack has done on your behalf"
      next="The full activity record — uploads, schedule changes, visibility changes — is next."
    />
  );
}
